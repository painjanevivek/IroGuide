# IroGuide backend

Standalone Fastify API for review generation and improvement planning.

```powershell
npm install
npm run dev
```

The service runs on `http://localhost:4000` by default. `GET /health` reports readiness.
