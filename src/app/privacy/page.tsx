import Link from "next/link";
export default function PrivacyPage() { return <div className="simple-page"><header className="simple-header"><Link href="/" className="wordmark"><span className="wordmark-mark">I</span>IroGuide</Link><Link href="/">Home</Link></header><main className="policy-main"><p className="eyebrow">Privacy baseline</p><h1>Your work remains yours.</h1><p>The review flow sends your uploaded image bytes and brief to the authenticated review API so the configured vision provider can analyze the actual pixels. Local demo mode is only a fallback when production credentials are not configured.</p><h2>Production requirements</h2><ul><li>Private, encrypted object storage with short-lived access URLs.</li><li>No public gallery or model-training use without separate explicit consent.</li><li>Deletion controls for designs, reviews, exports, and accounts.</li><li>Minimal privacy-safe logs that exclude images, briefs, signed URLs, and AI responses.</li></ul><p>A complete legal privacy notice must be reviewed and published before a live service launches.</p></main></div>; }
